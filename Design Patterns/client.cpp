#include<iostream>
#include "car.hpp"
#include "bike.hpp"

using namespace std;

int main ()
{
    string vehicleType;
    cin>>vehicleType;
    if(vehicleType == "car"){
        vehicle = new Car();
    }
    else if
    }
    return 0;
}